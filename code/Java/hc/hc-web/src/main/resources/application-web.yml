#配置springmvc的视图
spring:
  mvc:
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp
      
server:  
  port: 8090  #配置端口
  session-timeout: 30  
  tomcat.max-threads: 0  
  tomcat.uri-encoding: UTF-8
  servlet:
    context-path: /hc-web